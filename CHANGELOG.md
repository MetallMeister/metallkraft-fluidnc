# 変更履歴

## v0.1.13 / 2026-09-12

- 設定画面の「機能」「MetallKraftインターフェース」「FluidNC」に「標準に戻す」を追加。Wifiタブと上部ナビゲーションには表示しない。
- 利用者が正常な自機設定を標準として保存し、機械設定・画面設定・許可した機能設定を比較。差分を黄色で表示し、個別または一括で復元できるようにした。
- 復元前バックアップ、基板・FluidNC版の照合、Idle・M5・主軸回転数指令0の確認を追加。機械設定の適用は利用者が明示的に再起動するまで保留。
- Wi-Fi・通知などの接続設定、作業座標のゼロ点、SDファイル、ファームウェア、UI本体は復元対象外。機械固有の標準値とバックアップは配布物に含めない。
- 状態確認は標準UIの表示を使用し、比較操作のための追加WebSocketを開かない。実機と模擬環境で画面接続を維持することを確認。
- ベータ版。基準環境はMKS DLC32 MAX V1.0_002 / FluidNC v4.0.3。他の基板とFluidNC版は未検証。

## v0.1.12 / 2026-09-12

- 各ファイルのゴミ箱の右にダウンロードボタンを追加。個別保存は標準WebUIの処理を使用。
- 全ダウンロードを追加。表示中のSDフォルダの通常ファイルを1件ずつ保存。サブフォルダ・隠しファイルは対象外。取得失敗・サイズ不一致・通信不明・動作状態変更時は残りを中断。加工中は無効。
- 重複した上部サポートメニューと、待機に戻った後の「運転を始める前に」案内を非表示。アラームなどの復旧案内は維持。
- 操作ロック・パスワード機能は含めない。機械設定・登録マクロは変更なし。
- 模擬環境で個別保存の内容、全保存、失敗時の中断、加工中の無効化、既存削除処理を検証。

## v0.1.11 / 2026-09-11

- 短い間隔・同時刻に届いた位置報告で、未着色区間を飛ばして追跡位置だけ進む不具合を修正。次の移動報告で連続性・時間・距離が確認できた場合のみ区間を回復。
- 停止・不明座標・通信欠落・不連続経路をまたいでは回復しない。静止して待つだけでは着色しない。
- 直線・円弧で回帰テストを追加。プレビューのみ変更し、標準通信・加工制御・設定は維持。

## v0.1.10 / 2026-09-11

- 通過推定で、追跡済み位置から到達し得る範囲を優先して照合。遠い将来の重複経路によって現在の通過区間まで白く残る問題を改善。
- 近接した重複候補・通信欠落・不連続な移動は引き続き塗らない。SD読み込み率を加工済み距離には使用しない。
- プレビューのみ更新。標準画面の制御・通信・機械設定は変更なし。

## v0.1.9 / 2026-09-11

- 加工ファイルの選択ボタン右にゴミ箱を表示。SDファイルのチェック選択・全選択・一括削除を追加。
- 単独・一括とも対象名を表示して削除確認。標準の削除処理を1件ずつ使用し、失敗・通信不明・動作状態変更時は残りを中断。
- 削除対象は通常のSDファイルのみ。フォルダ・隠しファイル・Flashは対象外。実機SDの削除試験は行わず、模擬環境で検証。
- 画面の選択・確認処理を追加。標準通信・機械設定・登録マクロは維持。

## v0.1.8 / 2026-09-11

- 経路の凡例の右に2D・3Dのドラッグ操作と全体表示の短い説明を追加。プレビューのHTML・CSSのみ変更。

## v0.1.7 / 2026-09-11

- プレビューに平行移動を追加。2Dはドラッグ、3Dは右ドラッグ・中ボタンドラッグ・Shift＋ドラッグ。通常の3Dドラッグ回転は維持。
- 全体表示・ファイル切替で移動量をリセット。工具位置・通過経路も同じ表示位置へ移動し、機械座標や制御命令は変更しない。
- 標準画面本体・通信・機械設定は変更せず、プレビューファイルのみ更新。

## v0.1.6 / 2026-09-11

- 加工・一時停止・復帰動作中はマクロ、手動コマンド入力、送信ボタンを無効化して灰色表示。標準のコマンド生成・送信処理は変更せず、入力の利用可否だけを変更。
- 工具位置を青い点に戻し、プレビューの拡大・縮小・解析中止ボタンを非表示。既存のホイール拡縮・3Dドラッグ回転・全体表示は維持。
- 通過推定の灰色を濃くして白い予定経路との区別を改善。SD報告からの描画を模擬検証。判別不能な重複経路や未観測区間は塗らない方針を維持。
- 詳細メニューをプローブと同じ矢印・枠・配色に統一。

## v0.1.5 / 2026-09-11

- 接続が切れないリセット・アラームの後も、待機時に報告単位と座標系を再取得して工具位置表示を復帰。通常の連続加工とファイル再選択も模擬検証。
- 工具位置を青い縦棒で表示。棒の下端が基板報告座標を示す（実測位置ではない）。
- 復帰案内・主軸動作中・加工中の点滅を1.6秒の滑らかな周期に統一。復帰対象はボタンの面全体を強調。
- 標準画面の実行コード・機械設定・登録マクロは変更なし。変更はプレビューとCSSのみ。

## v0.1.4 / 2026-09-11

- 通常画面のスリープボタンを非表示にし、一時停止・リセット・アラーム解除を3等分で拡大。外部からスリープした場合の復旧案内は維持。
- 主軸は設定回転数を上、開始・停止ボタンを下に配置。
- 主軸・加工ファイル・現在位置・通信ログ・マクロに共通の見出しと区切りを追加。
- 変更はCSSのみ。JavaScript、通信、YAML、登録マクロは維持。短いデスクトップ画面でも移動ボタンを40px以上に確保。

## v0.1.3 / 2026-09-11

- 復旧案内と次に押すボタンの枠を、1.6秒周期で点滅。文字・位置・押せる範囲は固定。OSの動きを減らす設定では常時強調。
- スリープとクイック停止の用途・保持解除・復帰方法を、1秒ホバーの説明に追記。
- 変更はCSSのみ。画面JavaScript、通信・送信処理、YAML、登録マクロは変更なし。

## v0.1.2 / 2026-09-11

- 加工中はXY/Zの移動停止を無効化して灰色表示。ジョグ中の標準停止コマンドは維持。
- SDカード欄の正常時の「OK」を非表示。未挿入などのエラーは表示。
- クイック停止・スリープ・アラーム・一時停止の復帰案内を左下に表示し、次の操作を青枠で案内。案内自体はコマンドを送信せず、自動解除・自動運転なし。
- 機械原点復帰と主軸開始の間隔を詰め、余白をマクロ欄に配分。お知らせ下の余白を削除。
- 画面JavaScriptの差分は復帰案内、移動停止の操作可否、正常ステータスの表示属性。通信・送信処理、YAML、登録マクロは変更なし。

## v0.1.1 / 2026-09-11

- 主軸開始と停止を同じ位置に集約。基板の主軸指令がM3/M4なら停止ボタンが点滅し、M5なら開始ボタンを表示。
- 回転開始・停止のコマンドと状態判定は既存WebUIを使用。クリックだけで回転中表示へ切り替えない。動きを減らすOS設定では点滅しない。
- XYの十字配置は正方形のまま、XY/Z間にボタン半個分の余白を追加。
- マクロを主軸操作の下、加工開始の上へ移動。最大6個、7個目から枠内スクロール。
- 送り・主軸回転数指令の表示を座標の下へ移動。
- CSSと日本語ラベルのみ変更。画面本体のJavaScript、YAML、公開用初期設定はv0.1.0と同一。

## v0.1.0 / 2026-09-11

- MetallKraft操作画面、未校正のMKS DLC32 MAX初期YAML、日本語導入・復元手順を初公開。
